{
  "name": "pretext-asteroids-mp",
  "version": "0.1.0",
  "private": true,
  "description": "Multiplayer asteroids with Geckos.io - low latency WebRTC gameplay",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"bun run --filter server dev\" \"bun run --filter client dev\"",
    "build": "bun run --filter client build && bun run --filter server build",
    "start": "bun run --filter server start",
    "lint": "bun run --filter '*' lint"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest",
    "concurrently": "^8.2.2",
    "typescript": "^5.4.0"
  }
}
